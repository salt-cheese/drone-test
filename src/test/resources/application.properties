# DATASOURCE
spring.datasource.url = ${HELLO_APP_DB_TEST_URL:jdbc:mysql://localhost:3306/hello_test}
spring.datasource.username = ${HELLO_APP_DB_TEST_USERNAME:root}
spring.datasource.password = ${HELLO_APP_DB_TEST_PASSWORD:root}

# FLYWAY
spring.flyway.locations = classpath:db/migration, classpath:db/dml-test
